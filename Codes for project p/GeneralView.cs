using UnityEngine;

public class GeneralView : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Ref code
        // Init code
        // Load handler
        // Popup handler
    }
}
